//Destination array's element must be gearter or equal to 0 and smaller than 1;
class Linkedlist
{
//linked-list
};
double* Bucket_Sort(double *A,int length)
{
	//array that for changing element must be a linked-list or Two-dimensional-arrays;
	double **B = new double *[length];
	for (int i = 0; i < length; i++)
	{
		B[i] = new double[length];
	}//Two-dimensional-arrays;
}